<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dsbath.admin.model.mapper.NoticeMapper">

	<!-- 공지사항 등록 -->
	<insert id="insert" parameterType="notice">
		INSERT INTO Notice
		(
			AdminIdx,
			Title,
			Content,
			Hit,
			IsImport,
			IsPopup,
			IsComment,
			PopupStartDate,
			PopupEndDate
		)
		VALUES
		(
			#{adminIdx},
			#{title},
			#{content},
			0,
			#{isImport},
			#{isPopup},
			#{isComment},
			#{popupStartDate},
			#{popupEndDate}
		)
	</insert>
	
	<!-- 공지사항 목록 조회 -->
	<select id="select" parameterType="com.dsbath.admin.model.dto.PagingDTO" resultType="notice">
		SELECT
			AdminIdx,
			Title,
			Content,
			Hit,
			IsImport,
			IsPopup,
			IsComment,
			PopupStartDate,
			PopupEndDate
			
		 FROM Notice
	</select>
	
</mapper>